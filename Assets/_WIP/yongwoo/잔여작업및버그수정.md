# 잔여 작업 및 버그 수정 체크리스트

마감: 다음주 (STATUS.md 기준). 보스/엔딩은 본진 끝난 뒤 여유분.

**병렬 규칙**
- 같은 Day 블록 안 항목은 별도 표기 없으면 **서로 독립 → 병렬 가능**
- `→ X` 는 X 끝나야 시작
- `[검증묶기]` 표시는 같은 플레이세션에 검증 묶을 것

작업 완료 시 STATUS.md "남은 작업" 체크박스도 같이 갱신.

---

## Day 1 — 짧은 코드 버그 (회귀 위험 낮음, 병렬 진행)

> 시작 전: **미커밋 변경 체크포인트 커밋** (STATUS.md 경고).

- [x] **B-4 슬로우 중 대시 속도** — 대시 코루틴 `Time.unscaledDeltaTime` → `Time.deltaTime`
  - 검증: 우클릭 슬로우 중 대시 같이 느려짐
  - 주의: STATUS의 "질풍참 스타일 변경"이 같은 코루틴 → 임시 fix로만

- [x] **B-1 낙사 트리거** — 씬 하단 Trigger Collider("DeathZone") → `PlayerInteraction.OnDie()`
  - 부활: 기존 자동 리스폰(0.75s) 재사용. 새 체크포인트 시스템 안 만듦
  - 검증: 플랫폼 끝에서 떨어지면 사망 → 마지막 스폰 포인트

- [x] **B-5 글리치 무한 지속** — `ScreenGlitchOverlay.ResetGlitch()` (코루틴 정지 + Intensity=0)
  - 호출 지점: 시간 제어 3층 진입(FreezeTime / LockPlayer) + `SceneEventSequence` 중단/완료
  - 호출 지점 어디 둘지는 코드 보고 확정 (추측 금지)
  - 검증: 글리치 중 적 카메라 포커싱 트리거 → 사라짐

---

## Day 2 — 적 / 물리 (회귀 위험, [검증묶기])

### 적 초기 방향 (A안) — 완료

- [x] DeadRevolverEnemyController에 `InitialFacingDirection` enum + 인스펙터 필드, Awake에서 `_facing` 초기값 세팅
- [x] BlindHuntressEnemyBrain에 동일 적용
- [x] 4종 동작 회귀 없음 + 씬 배치 적용 검증 완료

### 나머지

- [x] **B-2 평지 끼임** — **재현 조건 좁히기 먼저** (어느 평지, 타일맵 이음새인지)
  - 후보: 콜라이더 모서리 걸림(Box → Capsule/Composite), friction. friction=0은 경사 미끄러짐 부작용 — 신중
  - 검증: 평지 좌우 끝 반복 이동, 점프 후 착지 컨트롤 회귀 없음 [검증묶기]

- [x] **B-3 적 낙하 방지** — `→ 적 초기 방향`
  - 진행 방향 앞 지면 raycast → 없으면 방향 전환/정지
  - 한 종(Brawler)부터 적용 → 동작 확인 → 나머지 확대
  - facing 표현을 "적 초기 방향"과 공유하도록 묶기
  - 검증: 4종 다 플랫폼 끝에서 멈춤/반전, 추적·공격 거리 회귀 없음 [검증묶기]

---

## Day 3 — 일시정지 (단일 트랙)

- [x] **F-1 일시정지** — `ESC` + 우상단 버튼 → 같은 함수
  - `Time.timeScale=0` + Input ActionMap → `UI` 전환 + 패널
  - **이미지 placeholder로 기능부터** (이미지는 Day 4)
  - **결정 필요:** 컷씬 중 ESC 막을지/허용할지, FreezeTime 중 ESC 동작
  - 검증: 게임 중 ESC 정지/해제, 컷씬 중 동작 정의대로, FreezeTime 중 동작 정의대로 [검증묶기 Day 2와 묶기 가능]
  - B-5 ResetGlitch를 일시정지 진입 시에도 호출할지 결정

---

## Day 4 — 아트 통합 (병렬, 단 애니는 Day 2 끝난 뒤)

- [x] **주인공 애니메이션 교체** `→ Day 2`
  - Animator state별 클립 swap
  - 공격 모션 프레임 수 바뀌면 `SlashHitbox` 활성 윈도우 재조정
  - 검증: 좌클릭 공격 타이밍 변화 없음, 적 4종 데미지 정상

- [x] **NPC 이미지 + 브로커 얼굴** — 4종(브로커/상인/로봇/행인) 스프라이트
  - **선결: 각 NPC 상호작용 동사 확정** (브로커=원격 통신, 상인=거래?, 로봇=?, 행인=?)
  - 브로커는 통신용 고해상도 headshot 별도
  - 기존 `Interactable` 위에 얹기 — 새 시스템 안 만듦
  - 검증: Idle/Talk 전환, 브로커 통신 시 CommsPanel headshot

- [x] **일시정지 UI 이미지** `→ F-1`
  - F-1 placeholder 위에 사이버펑크 톤 교체
  - 검증: 시각만, 기능 회귀 없음

---

## Day 5+ — 보너스 (시간 남으면만)

- [ ] **보스스테이지 + 보스** — STATUS.md "스코프 아웃: 보스"와 정합
  - 진입 시 **1페이즈 1패턴**으로 자르기. 페이즈 분기/슬로우 역이용은 더 남으면
  - 검증: 보스 패배 → 엔딩 또는 페이드아웃

- [ ] **엔딩** — 두 분기 준비
  - (a) 보스 처치 후 시네마틱
  - (b) 보스 없이 마지막 대화 + 페이드아웃 (보스 컷 시 fallback)

---

## 메모

- 진단 모호 항목(B-2 원인, B-5 호출 지점, F-1 컷씬 중 동작)은 **코드 보면서 확정** — 추측 금지
- B-2 / B-3 / F-1 / 적 초기 방향 검증은 같은 플레이세션에 묶기
